<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p>
<%= @task.completed %>
</p>
 <%= link_to "✍️ Update ✍️", edit_path %>
 <br>
 <%= link_to "🔙 Back", tasks_path %>
<br>
<%= link_to "❌ Delete ❌", task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
